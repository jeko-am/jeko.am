[    2547ms] [INFO] %cDownload the React DevTools for a better development experience: https://reactjs.org/link/react-devtools font-weight:bold @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/compiled/react-dom/cjs/react-dom.development.js:38559
[    2930ms] [ERROR] Warning: Prop `%s` did not match. Server: %s Client: %s%s href "/auth/signup" "/profile" 
    at a
    at LinkComponent (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/link.js:121:19)
    at div
    at div
    at div
    at section
    at WhatIsPure (webpack-internal:///(app-pages-browser)/./src/components/WhatIsPure.tsx:18:11)
    at div
    at main
    at Home (Server)
    at InnerLayoutRouter (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/layout-router.js:243:11)
    at RedirectErrorBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/redirect-boundary.js:74:9)
    at RedirectBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/redirect-boundary.js:82:11)
    at NotFoundErrorBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/not-found-boundary.js:76:9)
    at NotFoundBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/not-found-boundary.js:84:11)
    at LoadingBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/layout-router.js:349:11)
    at ErrorBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/error-boundary.js:160:11)
    at InnerScrollAndFocusHandler (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/layout-router.js:153:9)
    at ScrollAndFocusHandler (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/layout-router.js:228:11)
    at RenderFromTemplateContext (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/render-from-template-context.js:16:44)
    at OuterLayoutRouter (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/layout-router.js:370:11)
    at CartProvider (webpack-internal:///(app-pages-browser)/./src/lib/cart-context.tsx:48:11)
    at AuthProvider (webpack-internal:///(app-pages-browser)/./src/lib/auth.tsx:109:11)
    at LangProvider (webpack-internal:///(app-pages-browser)/./src/lib/i18n/LangProvider.tsx:52:11)
    at body
    at html
    at RootLayout (Server)
    at RedirectErrorBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/redirect-boundary.js:74:9)
    at RedirectBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/redirect-boundary.js:82:11)
    at NotFoundErrorBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/not-found-boundary.js:76:9)
    at NotFoundBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/not-found-boundary.js:84:11)
    at DevRootNotFoundBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/dev-root-not-found-boundary.js:33:11)
    at ReactDevOverlay (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/ReactDevOverlay.js:87:9)
    at HotReload (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:321:11)
    at Router (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/app-router.js:207:11)
    at ErrorBoundaryHandler (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/error-boundary.js:113:9)
    at ErrorBoundary (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/error-boundary.js:160:11)
    at AppRouter (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/app-router.js:585:13)
    at ServerRoot (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:112:27)
    at Root (webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:117:11) @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  348021ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  348939ms] [LOG] [Fast Refresh] done in 918ms @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:43
[  392904ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  393067ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  413368ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  413940ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  414364ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  423402ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  423881ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  424228ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  430465ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  430468ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  430786ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  439775ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  439780ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  440593ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  461449ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  470004ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  475046ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  492127ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  492231ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  493526ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  500241ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  500688ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  500981ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  507166ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  507880ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  508280ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  514661ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  514663ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  515107ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  522664ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  522740ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  523104ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  542884ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  542889ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  544345ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx
./src/app/benefits/hypoallergenic/page.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  600515ms] [ERROR] ./src/components/HealthConditionPage.tsx
Error: 
  x the name `symptoms` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:51:1]
  51 | 
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
     :     ^^^^|^^^
     :         `-- previous definition of `symptoms` here
  55 |     foodBenefits,
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
     :         ^^^^|^^^
     :             `-- `symptoms` redefined here
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
     `----

  x the name `foodBenefits` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:52:1]
  52 | export default function HealthConditionPage(props: HealthConditionProps) {
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
     :     ^^^^^^|^^^^^
     :           `-- previous definition of `foodBenefits` here
  56 |     faqs,
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
     :         ^^^^^^|^^^^^
     :               `-- `foodBenefits` redefined here
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
     `----

  x the name `faqs` is defined multiple times
     ,-[/Users/admin/Desktop/claude code armen/pure-pet-clone/src/components/HealthConditionPage.tsx:53:1]
  53 |   const {
  54 |     symptoms,
  55 |     foodBenefits,
  56 |     faqs,
     :     ^^|^
     :       `-- previous definition of `faqs` here
  57 |     accentColor = "#274C46",
  58 |     pageSlug,
  59 |   } = props;
  60 |   const signupUrl = useSignupUrl();
  61 |   const { t, lang } = useT();
  62 | 
  63 |   // Admin overrides loaded from page_sections (admin store editor writes here).
  64 |   // Each section's content is keyed by `_section_index` (0=Hero, 1=What Is, 2=How Diet Helps, 3=Testimonial).
  65 |   const [overrides, setOverrides] = useState<Record<number, Record<string, unknown>> | null>(null);
  66 |   useEffect(() => {
  67 |     if (!pageSlug) return;
  68 |     let cancelled = false;
  69 |     (async () => {
  70 |       try {
  71 |         const withSlash = pageSlug.startsWith('/') ? pageSlug : `/${pageSlug}`;
  72 |         const without = pageSlug.startsWith('/') ? pageSlug.slice(1) : pageSlug;
  73 |         const { data: pages } = await supabase
  74 |           .from('pages')
  75 |           .select('id')
  76 |           .or(`slug.eq.${withSlash},slug.eq.${without}`)
  77 |           .limit(1);
  78 |         const pageId = pages?.[0]?.id;
  79 |         if (!pageId) return;
  80 |         const { data: sections } = await supabase
  81 |           .from('page_sections')
  82 |           .select('content')
  83 |           .eq('page_id', pageId);
  84 |         const map: Record<number, Record<string, unknown>> = {};
  85 |         sections?.forEach((s: { content: Record<string, unknown> | null }) => {
  86 |           const idx = (s.content?._section_index ?? s.content?._homepage_index) as number | undefined;
  87 |           if (typeof idx === 'number') map[idx] = s.content || {};
  88 |         });
  89 |         if (!cancelled) setOverrides(map);
  90 |       } catch {
  91 |         // Ignore — fall back to props.
  92 |       }
  93 |     })();
  94 |     return () => { cancelled = true; };
  95 |   }, [pageSlug]);
  96 | 
  97 |   // Resolve a string field with locale-aware override lookup.
  98 |   const fieldStr = (idx: number, key: string, fallback: string): string => {
  99 |     const o = overrides?.[idx];
 100 |     if (!o) return fallback;
 101 |     if (lang === 'hy') {
 102 |       const hy = (o.hy as Record<string, unknown> | undefined)?.[key];
 103 |       if (typeof hy === 'string' && hy.length > 0) return hy;
 104 |     }
 105 |     const en = o[key];
 106 |     if (typeof en === 'string' && en.length > 0) return en;
 107 |     return fallback;
 108 |   };
 109 | 
 110 |   // Section indices after schema expansion:
 111 |   // 0=Hero, 1=What Is, 2=Symptoms, 3=How Diet Helps, 4=Testimonial, 5=FAQs, 6=CTA
 112 |   const eyebrow = fieldStr(0, 'eyebrow', t('health.hero.eyebrow'));
 113 |   const condition = fieldStr(0, 'condition', props.condition);
 114 |   const tagline = fieldStr(0, 'tagline', props.tagline);
 115 |   const heroImage = fieldStr(0, 'hero_image', props.heroImage);
 116 |   const heroDescription = fieldStr(0, 'hero_description', props.heroDescription);
 117 |   const heroCta = fieldStr(0, 'hero_cta', t('health.hero.cta'));
 118 |   const whatIsTitle = fieldStr(1, 'what_is_title', props.whatIsTitle);
 119 |   const whatIsText = ((): string[] => {
 120 |     const t1 = fieldStr(1, 'what_is_text_1', '');
 121 |     const t2 = fieldStr(1, 'what_is_text_2', '');
 122 |     const t3 = fieldStr(1, 'what_is_text_3', '');
 123 |     const collected = [t1, t2, t3].filter(Boolean);
 124 |     return collected.length > 0 ? collected : props.whatIsText;
 125 |   })();
 126 |   const signsHeading = fieldStr(2, 'signs_heading', t('health.signs.heading'));
 127 |   const signsSubtitle = fieldStr(2, 'signs_subtitle', t('health.signs.subtitle'));
 128 |   const symptoms = ((): typeof props.symptoms => {
 129 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 130 |       icon: fieldStr(2, `symptom_${n}_icon`, ''),
 131 |       title: fieldStr(2, `symptom_${n}_title`, ''),
 132 |       description: fieldStr(2, `symptom_${n}_description`, ''),
 133 |     })).filter((s) => s.title || s.description);
 134 |     return fromDb.length > 0 ? fromDb : props.symptoms;
 135 |   })();
 136 |   const howDietHelpsTitle = fieldStr(3, 'how_diet_title', props.howDietHelpsTitle);
 137 |   const howDietAccent = fieldStr(3, 'how_diet_accent', t('health.diet.accent'));
 138 |   const howDietHelpsText = fieldStr(3, 'how_diet_text', props.howDietHelpsText);
 139 |   const foodImage = fieldStr(3, 'food_image', props.foodImage);
 140 |   const foodBenefits = ((): typeof props.foodBenefits => {
 141 |     const fromDb = [1, 2, 3, 4, 5].map((n) => ({
 142 |       title: fieldStr(3, `food_benefit_${n}_title`, ''),
 143 |       description: fieldStr(3, `food_benefit_${n}_description`, ''),
 144 |     })).filter((b) => b.title || b.description);
 145 |     return fromDb.length > 0 ? fromDb : props.foodBenefits;
 146 |   })();
 147 |   const testimonial = {
 148 |     quote: fieldStr(4, 'testimonial_quote', props.testimonial.quote),
 149 |     name: fieldStr(4, 'testimonial_name', props.testimonial.name),
 150 |     image: fieldStr(4, 'testimonial_image', props.testimonial.image),
 151 |   };
 152 |   const faqsHeading = fieldStr(5, 'faqs_heading', t('health.faqs.heading'));
 153 |   const faqs = ((): typeof props.faqs => {
     :         ^^|^
     :           `-- `faqs` redefined here
 154 |     const fromDb = [1, 2, 3, 4, 5, 6].map((n) => ({
 155 |       question: fieldStr(5, `faq_${n}_question`, ''),
 156 |       answer: fieldStr(5, `faq_${n}_answer`, ''),
     `----

Import trace for requested module:
./src/components/HealthConditionPage.tsx @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/app-index.js:32
[  600517ms] [LOG] [Fast Refresh] rebuilding @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:186
[  604531ms] [LOG] [Fast Refresh] done in 4013ms @ webpack-internal:///(app-pages-browser)/./node_modules/next/dist/client/components/react-dev-overlay/app/hot-reloader-client.js:43
